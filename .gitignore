# Compiled game binary
tetrisplus
# ...and the name it had before the rename, so a stale build cannot sneak in.
tetris

# Object files and archives
*.o
*.a
*.so

# Editor / IDE noise
*.swp
*~
.vscode/
.idea/

# Python bytecode from the test suite
__pycache__/

# Build and debug leftovers
*.dSYM/
core

# .deb output from build-deb.sh
dist/

# Snapcraft output from `snapcraft pack` (driven by snap/snapcraft.yaml).
# It builds in the project root, so these land next to the source.
parts/
stage/
prime/
.craft/
*.snap

# Local session export / scratch notes -- never commit these
tetris.txt
